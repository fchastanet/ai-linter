---
coverage:
  status:
    patch:
      default:
        target: 80%  # All new code must be at least 80% covered
    project:
      default:
        target: auto   # PRs must maintain or increase overall coverage
        threshold: 0%  # Allow no drop in coverage (adjust for flaky tests)
  ignore:
    - "*_test.py"
